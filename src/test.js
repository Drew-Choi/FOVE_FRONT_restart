// 테스트용
// const prevHandleRef = useRef(buyNow);
// useEffect(() => {
//   if (prevHandleRef.current !== buyNow) {
//     console.log('handle function has changed');
//     prevHandleRef.current = buyNow;
//   }
// }, [buyNow]);
